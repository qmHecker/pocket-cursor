# Create a Telegram bot via @BotFather and paste the token here
TELEGRAM_BOT_TOKEN=your-telegram-bot-token

# Optional: OpenAI API key for voice message transcription
# Without this, voice messages won't be transcribed
OPENAI_API_KEY=your-openai-api-key

# Optional: ElevenLabs API key for text-to-speech voice messages
# ELEVENLABS_API_KEY=your-elevenlabs-api-key

# Optional: Pronunciation dictionary for voice messages
# Fixes mispronounced words (e.g. names across languages). Create a PLS file
# (see scripts/pronunciation.example.pls), upload via ElevenLabs API or dashboard,
# then set the dictionary ID, version, and the word that should use it.
# ELEVENLABS_PRONUNCIATION_DICT_ID=your-dict-id
# ELEVENLABS_PRONUNCIATION_DICT_VERSION=your-version-id
# ELEVENLABS_PRONUNCIATION_WORD=Michael

# Optional: Context monitor — annotates your chat input with context window fill level
# When enabled, PocketCursor reads the context % after each turn and prefills an annotation
# into the input field. The AI decides what to do with it — save working memory, write a
# journal entry, or just note it. Requires a Cursor rule — see README for setup.
# CONTEXT_MONITOR=true

# Optional: Local render directory for phone outbox images
# If the repo is on a network drive with high latency (e.g. NAS over VPN),
# Node.js loading dependencies can be slow. Point this to a local directory.
# Run: python setup_local_render.py
# RENDER_LOCAL_DIR=Q:\.pocket-cursor\render
